This repo contains some examples and mini projects for Large Language Models.

## Setup environment using poetry
1. Clone the repository: `git clone https://github.com/Waqar351/LLMs_Mini_Projects.git`
2. Navigate to the project directory: `cd repository-name`
3. Install dependencies: `poetry install`
4. Activate the environment: `poetry shell`
